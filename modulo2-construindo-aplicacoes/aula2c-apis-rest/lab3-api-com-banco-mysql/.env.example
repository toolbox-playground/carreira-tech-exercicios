# .env.example — este arquivo VAI ao GitHub (sem valores reais)
# Copie para .env antes de rodar:
#   cp .env.example .env

DB_HOST=127.0.0.1
DB_PORT=3308
DB_USER=appuser
DB_PASSWORD=senha123
DB_NAME=loja
